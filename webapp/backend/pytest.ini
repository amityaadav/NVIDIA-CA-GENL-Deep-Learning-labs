[pytest]
# Tests import the app modules (model, inference, app) directly, so put the
# backend dir on the path. Keeps imports flat and matches how the app runs.
pythonpath = .
testpaths = tests
addopts = -q
